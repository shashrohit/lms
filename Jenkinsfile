properties([pipelineTriggers([githubPush()])])
node {
    stage ('Checkout'){
        git branch: 'master', url: 'https://github.com/shashrohit/lms.git'
    }
    stage ('Build'){
        // steps
    }
    stage ('Test'){
        // steps
    }
}